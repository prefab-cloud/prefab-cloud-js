{
  "name": "prefab-cloud-js",
  "version": "0.1.0",
  "description": "FeatureFlags & Dynamic Configuration as a Service",
  "main": "dist/index.js",
  "author": "Jeffrey Chupp",
  "license": "ISC",
  "devDependencies": {
    "@types/jest": "^28.1.6",
    "@typescript-eslint/eslint-plugin": "^5.33.0",
    "@typescript-eslint/parser": "^5.33.0",
    "eslint": "^8.21.0",
    "eslint-config-airbnb-base": "^15.0.0",
    "eslint-plugin-import": "^2.26.0",
    "eslint-plugin-jest": "^26.8.0",
    "jest": "^28.1.3",
    "ts-node": "^10.9.1",
    "typescript": "^4.7.4"
  },
  "scripts": {
    "build": "./build.sh",
    "test": "npm run build && jest --verbose dist/",
    "generate-ts-types": "tsc --declaration --allowJs --emitDeclarationOnly --outDir types --project tsconfig.build.json",
    "release": "npm run build && npm run generate-ts-types && rm -rf dist/test/ dist/jest.config.js dist/src/*.test.js"
  }
}
